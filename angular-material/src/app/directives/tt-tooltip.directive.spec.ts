import { ttTooltipDirective } from './tt-tooltip.directive';

describe('ttTooltipDirective', () => {
  it('should create an instance', () => {
    // const directive = new ttTooltipDirective();
    // expect(directive).toBeTruthy();
  });
});
