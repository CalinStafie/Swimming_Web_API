import { ElementRef } from '@angular/core';
import { HighlightPurchaseDirective } from './highlight-purchase.directive';

describe('HighlightPurchaseDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightPurchaseDirective();
    // expect(directive).toBeTruthy();
  });
});
